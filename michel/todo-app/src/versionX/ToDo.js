export default class ToDo {
    constructor(id, title, completed, rating, userName)
    {
        this.id = id;
        this.title = title;
        this.completed = completed;
        this.rating = rating;
        this.userName = userName;
    }
}