export default class ToDo {
    constructor(id, title, completed, rating)
    {
        this.id = id;
        this.title = title;
        this.completed = completed;
        this.userId = rating;
    }
}